
import 'package:flutter/material.dart';

class AddNewRepresentativeView extends StatelessWidget {
  const AddNewRepresentativeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}